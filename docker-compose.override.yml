version: '3'
services:
  web:
    build: .
    environment:
      APP_ENV: development
      VIRTUAL_HOST: localhost
    ports:
      - "3000:3000"
  scheduler:
    build: .
    environment:
      APP_ENV: development
